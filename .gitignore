__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.venv/
venv/
.env
*.db
.DS_Store
.idea/
.vscode/

# S3-synced content is each student's own; don't commit
S3_content/*
!S3_content/.gitkeep

# Cursor IDE artifacts
.cursorindexingignore
.specstory/

# Coverage and lint caches
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Flask instance folder (per-app config / sqlite, etc.)
instance/

# Local logs (e.g. audit log output if the team wires a FileHandler)
*.log

# Week 8 — self-signed TLS material (§7); regenerate per environment
nginx/certs/*.pem

# Local-only: assignment/study guides and duplicate contract mirror (not for team remote)
docs/
week8_study_guide-final.md
contracts/CONTRACTS.md
